[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "mvgrid-osm-schneider"
version = "0.1.0"
description = "OSM-based MV grid reconstruction with a unified Novi Sad planning model"
keywords = ["ev-charging", "power-grid-simulation", "pandapower", "openstreetmap", "reinforcement-learning", "mcp"]
readme = "README.md"
requires-python = ">=3.10,<3.13"
license = {file = "LICENSE"}
authors = [
  {name = "Original upstream contributors"},
  {name = "Schneider challenge contributors"},
]
dynamic = ["dependencies"]

[project.urls]
Repository = "https://github.com/Grkila/gridlab-ev-grid-simulator"
Documentation = "https://github.com/Grkila/gridlab-ev-grid-simulator/blob/main/docs/USER_GUIDE.md"
Issues = "https://github.com/Grkila/gridlab-ev-grid-simulator/issues"

[project.scripts]
mvgrid-novi-sad = "mvgrid.novi_sad.pipeline:main"
mvgrid-legacy-gui = "mvgrid.legacy.main:main"
mvgrid-ev-mcp = "mvgrid.novi_sad.playground.mcp_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
