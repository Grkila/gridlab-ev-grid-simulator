# Test rules

- Tests must be deterministic and offline by default.
- Keep fast repository and artifact checks separate from the full Novi Sad workflow.
- Test launchers from outside the repository when path handling changes.
- Do not replace electrical-model validation with shallow file-existence checks.
