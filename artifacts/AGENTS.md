# Artifact rules

- Artifacts are generated outputs, not hand-maintained source.
- Keep only one immutable Novi Sad reference set in Git and update its manifests and validation reports together.
- A changed model requires a full validation run and an updated project-memory baseline.
- Prefer a versioned GitHub Release for future large or alternate output sets.
