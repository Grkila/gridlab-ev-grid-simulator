# Configuration rules

- `novi_sad.json` is the default active configuration.
- `darmstadt.example.json` documents the upstream example and is not the default.
- Preserve keys used by the legacy stages; configuration schema changes require tests and documentation updates.
- Select another configuration with the `MVGRID_CONFIG` environment variable.
